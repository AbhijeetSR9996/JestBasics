/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import FlashMsg from './src/FlashMsg';

AppRegistry.registerComponent(appName, () => FlashMsg);

